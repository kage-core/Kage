---
type: "Gotcha"
title: "Provider usage.input_tokens is the UNCACHED remainder — never compare it to count_tokens"
description: "In the Anthropic API, response usage.input tokens is the UNCACHED REMAINDER ONLY. The full prompt size is input tokens + cache creation input tokens + cache read input tokens. The /v1/messages/count tokens endpoint, by c"
resource: "mcp/vnext/measurement/token-count.ts"
tags: ["session-learning", "vnext", "measurement", "proxy", "receipts", "phase-a", "anthropic-api"]
timestamp: "2026-07-14T20:57:28.488Z"
x-kage-id: "repo:kage-vnext-implementation:gotcha:provider-usage-input-tokens-is-the-uncached-remainder-never-compare-it-to-count-"
x-kage-type: "gotcha"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-verified: "unverified"
x-kage-paths: ["mcp/vnext/measurement/token-count.ts", "mcp/vnext/measurement/pricing.ts", "mcp/vnext/measurement/receipt.ts", "mcp/vnext/adapters/anthropic-proxy.ts", "mcp/proxy.ts"]
---

# Provider usage.input_tokens is the UNCACHED remainder — never compare it to count_tokens

> In the Anthropic API, response usage.input tokens is the UNCACHED REMAINDER ONLY. The full prompt size is input token…

In the Anthropic API, response `usage.input_tokens` is the UNCACHED REMAINDER ONLY. The full prompt size is input_tokens + cache_creation_input_tokens + cache_read_input_tokens. The /v1/messages/count_tokens endpoint, by contrast, returns the FULL token count of the body and knows nothing about caching.

These two numbers are therefore NOT commensurable, and Kage's vNext proxy receipts originally put one on each side of an "exact" measurement. Claude Code — the proxy's actual user — uses cache_control aggressively, so in steady state usage.input_tokens is a few hundred against a prompt of tens of thousands. The receipt would read: before 26,300 tokens / $0.13, after 420 tokens / $0.002, measurement_quality "exact" — i.e. Kage claiming that injecting context SAVED 98% of the prompt cost. In audit mode the same mechanism inverts and claims injection increased cost ~60x. That is an inference dressed as a measurement, and it is the exact failure the whole measurement phase exists to prevent.

Rules that follow, all now enforced in mcp/vnext/measurement/:
- The token quantity that goes on a receipt is totalPromptTokens = input + cache_creation + cache_read, which IS commensurable with count_tokens. If ANY component is absent or unparseable the total is null — never a partial sum, never defaulted to 0.
- Cost must be priced per component, because cached and uncached tokens bill at different rates (cache read ~0.1x, 5m cache write ~1.25x, 1h ~2x). Pricing a cached request as if all input were uncached is wrong. A side with a measured token count but NO measured cache breakdown (which is every count_tokens side) gets a NULL cost, not a cost computed as if uncached.
- Consequence for any aggregate: exact TOKEN deltas are available far more often than exact COST deltas, and a one-sided cost is UNUSABLE, not zero. Never conflate the two.
- /v1/messages/count_tokens rejects unrecognized top-level fields. Posting a full Messages body (with max_tokens, stream, metadata) 400s, so the probe silently returns null and every receipt stays "partial" forever while still costing a billable round trip. Send only model/messages/system/tools/tool_choice/thinking.
- Receipts are written only for requests that were ACTUALLY TRANSFORMED. A zero-recall request writes nothing, so exact/partial/unavailable coverage means "of transformed requests" — otherwise trivially-exact no-op rows drown the one number that matters.

Testing lesson: the original tests all used a fake upstream returning usage WITHOUT cache fields — the single regime where the two quantities coincide. A measurement test that never exercises a cached request cannot catch this class of bug.
Evidence: Found by review of commit 73c7a97 and fixed in 4fb4390. Every pre-existing test's fake upstream returned usage with no cache fields — the one regime where the two numbers happen to agree — which is exactly why the defect survived the first implementation and its tests.
Verified by: npm test --prefix mcp (620/620); node --test mcp/dist/vnext/measurement/measurement.test.js mcp/dist/proxy.test.js (45/45); dogfood 12/12

## Verification

Found by review of commit 73c7a97 and fixed in 4fb4390. Every pre-existing test's fake upstream returned usage with no cache fields — the one regime where the two numbers happen to agree — which is exactly why the defect survived the first implementation and its tests.

# Citations

[1] explicit_capture (2026-07-14T20:57:28.488Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:kage-vnext-implementation:gotcha:provider-usage-input-tokens-is-the-uncached-remainder-never-compare-it-to-count-","title":"Provider usage.input_tokens is the UNCACHED remainder — never compare it to count_tokens","summary":"In the Anthropic API, response usage.input tokens is the UNCACHED REMAINDER ONLY. The full prompt size is input tokens + cache creation input tokens + cache read input tokens. The /v1/messages/count tokens endpoint, by c","body":"In the Anthropic API, response `usage.input_tokens` is the UNCACHED REMAINDER ONLY. The full prompt size is input_tokens + cache_creation_input_tokens + cache_read_input_tokens. The /v1/messages/count_tokens endpoint, by contrast, returns the FULL token count of the body and knows nothing about caching.\n\nThese two numbers are therefore NOT commensurable, and Kage's vNext proxy receipts originally put one on each side of an \"exact\" measurement. Claude Code — the proxy's actual user — uses cache_control aggressively, so in steady state usage.input_tokens is a few hundred against a prompt of tens of thousands. The receipt would read: before 26,300 tokens / $0.13, after 420 tokens / $0.002, measurement_quality \"exact\" — i.e. Kage claiming that injecting context SAVED 98% of the prompt cost. In audit mode the same mechanism inverts and claims injection increased cost ~60x. That is an inference dressed as a measurement, and it is the exact failure the whole measurement phase exists to prevent.\n\nRules that follow, all now enforced in mcp/vnext/measurement/:\n- The token quantity that goes on a receipt is totalPromptTokens = input + cache_creation + cache_read, which IS commensurable with count_tokens. If ANY component is absent or unparseable the total is null — never a partial sum, never defaulted to 0.\n- Cost must be priced per component, because cached and uncached tokens bill at different rates (cache read ~0.1x, 5m cache write ~1.25x, 1h ~2x). Pricing a cached request as if all input were uncached is wrong. A side with a measured token count but NO measured cache breakdown (which is every count_tokens side) gets a NULL cost, not a cost computed as if uncached.\n- Consequence for any aggregate: exact TOKEN deltas are available far more often than exact COST deltas, and a one-sided cost is UNUSABLE, not zero. Never conflate the two.\n- /v1/messages/count_tokens rejects unrecognized top-level fields. Posting a full Messages body (with max_tokens, stream, metadata) 400s, so the probe silently returns null and every receipt stays \"partial\" forever while still costing a billable round trip. Send only model/messages/system/tools/tool_choice/thinking.\n- Receipts are written only for requests that were ACTUALLY TRANSFORMED. A zero-recall request writes nothing, so exact/partial/unavailable coverage means \"of transformed requests\" — otherwise trivially-exact no-op rows drown the one number that matters.\n\nTesting lesson: the original tests all used a fake upstream returning usage WITHOUT cache fields — the single regime where the two quantities coincide. A measurement test that never exercises a cached request cannot catch this class of bug.\nEvidence: Found by review of commit 73c7a97 and fixed in 4fb4390. Every pre-existing test's fake upstream returned usage with no cache fields — the one regime where the two numbers happen to agree — which is exactly why the defect survived the first implementation and its tests.\nVerified by: npm test --prefix mcp (620/620); node --test mcp/dist/vnext/measurement/measurement.test.js mcp/dist/proxy.test.js (45/45); dogfood 12/12","type":"gotcha","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["session-learning","vnext","measurement","proxy","receipts","phase-a","anthropic-api"],"paths":["mcp/vnext/measurement/token-count.ts","mcp/vnext/measurement/pricing.ts","mcp/vnext/measurement/receipt.ts","mcp/vnext/adapters/anthropic-proxy.ts","mcp/proxy.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-07-14T20:57:28.488Z"}],"context":{"fact":"In the Anthropic API, response `usage.input_tokens` is the UNCACHED REMAINDER ONLY. The full prompt size is input_tokens + cache_creation_input_tokens + cache_read_input_tokens. The /v1/messages/count_tokens endpoint, by contrast, returns the FULL token count of the body and knows nothing about caching.","verification":"Found by review of commit 73c7a97 and fixed in 4fb4390. Every pre-existing test's fake upstream returned usage with no cache fields — the one regime where the two numbers happen to agree — which is exactly why the defect survived the first implementation and its tests."},"freshness":{"ttl_days":365,"last_verified_at":"2026-07-14T20:57:28.488Z","path_fingerprints":[{"path":"mcp/vnext/measurement/token-count.ts","sha256":"c6cf6106a6faf7d858c0a58898a2a1ad831601211528f0e1856cf7a992ea4fd4","size":7067,"symbols":[{"name":"totalprompttokens","kind":"function","sha256":"0bd53d2273228ea40c79bc1bc1c81d437828fe3538840372fedd3590c3589f8f"}]},{"path":"mcp/vnext/measurement/pricing.ts","sha256":"937f37281c6b25eab7a295d48db34d85c93eef7d656edeb0ee69624017a9dff3","size":6712},{"path":"mcp/vnext/measurement/receipt.ts","sha256":"0dfac37560b5e50c1b6a4d091cc53d1d9a9d326fc0384ef53340d16934d5e6f5","size":3801},{"path":"mcp/vnext/adapters/anthropic-proxy.ts","sha256":"729e880cf37c25265fc57a4051930fd543821542c81a65d40302420ce623c5c8","size":9118},{"path":"mcp/proxy.ts","sha256":"3eceb78c59f4c50b0fa2c382a52587e4528043856b889594edb4ce953c29141f","size":22034}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"stale_reasons":[],"estimated_tokens_saved":776},"created_at":"2026-07-14T20:57:28.488Z","updated_at":"2026-07-14T20:57:28.488Z","author_branch":"codex/kage-vnext-implementation"}
```

