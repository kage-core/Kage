---
type: "Gotcha"
title: "ASSIST mode live-verified on subscription OAuth: mutated bodies are accepted, first delivered deliveries recorded"
description: "SETTLES the last open live question from the OAuth proxy saga: ASSIST mode — where the proxy MUTATES the request body by appending verified repo memory to the last user turn — is accepted by Anthropic on a Claude Code SU"
resource: "mcp/proxy.ts"
tags: ["session-learning", "proxy", "oauth", "subscription", "assist", "live-traffic", "injection"]
timestamp: "2026-07-16T17:14:02.910Z"
x-kage-id: "repo:kage-vnext-implementation:gotcha:assist-mode-live-verified-on-subscription-oauth-mutated-bodies-are-accepted-firs"
x-kage-type: "gotcha"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-verified: "unverified"
x-kage-paths: ["mcp/proxy.ts", "mcp/vnext/adapters/anthropic-proxy.ts", "mcp/vnext/storage/delivery-store.ts"]
---

# ASSIST mode live-verified on subscription OAuth: mutated bodies are accepted, first delivered deliveries recorded

> SETTLES the last open live question from the OAuth proxy saga: ASSIST mode — where the proxy MUTATES the request body…

SETTLES the last open live question from the OAuth proxy saga: ASSIST mode — where the proxy MUTATES the request body by appending verified repo memory to the last user turn — is accepted by Anthropic on a Claude Code SUBSCRIPTION (OAuth, not API key). Live-verified 2026-07-16 with a real headless Claude Code session (claude -p) through kage proxy --mode assist: 4 memories injected per eligible request, requests succeeded (no 401/429 from the mutation), the session answered normally, and the store recorded the first real "delivered" context deliveries (attachment_by_provider.anthropic delivered:2).

Combined with the same-day audit-mode verification (byte-identical forwarding, receipts 3→6, honest skips), the ORIGINAL 2026-07-07 hypothesis — that OAuth rejects mutated bodies with a 429 — is now DISPROVEN for last-user-turn injection. The historical 429s were genuine rate limiting. The one mutation that does remain forbidden is the SYSTEM prompt: injection deliberately targets the last user turn because OAuth identity checks are believed to cover `system` (never re-tested, and no reason to — the design never touches system).

Validation technique worth reusing: a headless `claude -p --model haiku "<one-shot prompt>"` with ANTHROPIC_BASE_URL pointed at the proxy is a cheap, complete end-to-end test — it exercises real OAuth auth, the full request/stream path, recall/injection, receipts, and deliveries in one command, with `kage status --project <dir> --json` as the assertion surface. Audit and assist can be validated against separate proxy ports on the same project store.
Evidence: Proxy stats: 2 req, 8 memories injected, 2 receipts, 54,078 in / 218 out tokens; the session completed normally (answered the prompt) with no 4xx from the mutated requests. kage status afterwards: attachment delivered:2 / skipped:6 / failed_open:0, attachment_by_provider.anthropic delivered:2 — the first real DELIVERED context deliveries. Same day, audit mode was validated the same way (receipts 3→6, all skipped) through a separate live session.
Verified by: Live run 2026-07-16: kage proxy --mode assist --port 8789 fronting api.anthropic.com; a real headless Claude Code session (claude -p, v2.1.211, subscription OAuth) driven through it end to end

## Verification

Proxy stats: 2 req, 8 memories injected, 2 receipts, 54,078 in / 218 out tokens; the session completed normally (answered the prompt) with no 4xx from the mutated requests. kage status afterwards: attachment delivered:2 / skipped:6 / failed_open:0, attachment_by_provider.anthropic delivered:2 — the first real DELIVERED context deliveries. Same day, audit mode was validated the same way (receipts 3→6, all skipped) through a separate live session.

# Citations

[1] explicit_capture (2026-07-16T17:14:02.910Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:kage-vnext-implementation:gotcha:assist-mode-live-verified-on-subscription-oauth-mutated-bodies-are-accepted-firs","title":"ASSIST mode live-verified on subscription OAuth: mutated bodies are accepted, first delivered deliveries recorded","summary":"SETTLES the last open live question from the OAuth proxy saga: ASSIST mode — where the proxy MUTATES the request body by appending verified repo memory to the last user turn — is accepted by Anthropic on a Claude Code SU","body":"SETTLES the last open live question from the OAuth proxy saga: ASSIST mode — where the proxy MUTATES the request body by appending verified repo memory to the last user turn — is accepted by Anthropic on a Claude Code SUBSCRIPTION (OAuth, not API key). Live-verified 2026-07-16 with a real headless Claude Code session (claude -p) through kage proxy --mode assist: 4 memories injected per eligible request, requests succeeded (no 401/429 from the mutation), the session answered normally, and the store recorded the first real \"delivered\" context deliveries (attachment_by_provider.anthropic delivered:2).\n\nCombined with the same-day audit-mode verification (byte-identical forwarding, receipts 3→6, honest skips), the ORIGINAL 2026-07-07 hypothesis — that OAuth rejects mutated bodies with a 429 — is now DISPROVEN for last-user-turn injection. The historical 429s were genuine rate limiting. The one mutation that does remain forbidden is the SYSTEM prompt: injection deliberately targets the last user turn because OAuth identity checks are believed to cover `system` (never re-tested, and no reason to — the design never touches system).\n\nValidation technique worth reusing: a headless `claude -p --model haiku \"<one-shot prompt>\"` with ANTHROPIC_BASE_URL pointed at the proxy is a cheap, complete end-to-end test — it exercises real OAuth auth, the full request/stream path, recall/injection, receipts, and deliveries in one command, with `kage status --project <dir> --json` as the assertion surface. Audit and assist can be validated against separate proxy ports on the same project store.\nEvidence: Proxy stats: 2 req, 8 memories injected, 2 receipts, 54,078 in / 218 out tokens; the session completed normally (answered the prompt) with no 4xx from the mutated requests. kage status afterwards: attachment delivered:2 / skipped:6 / failed_open:0, attachment_by_provider.anthropic delivered:2 — the first real DELIVERED context deliveries. Same day, audit mode was validated the same way (receipts 3→6, all skipped) through a separate live session.\nVerified by: Live run 2026-07-16: kage proxy --mode assist --port 8789 fronting api.anthropic.com; a real headless Claude Code session (claude -p, v2.1.211, subscription OAuth) driven through it end to end","type":"gotcha","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["session-learning","proxy","oauth","subscription","assist","live-traffic","injection"],"paths":["mcp/proxy.ts","mcp/vnext/adapters/anthropic-proxy.ts","mcp/vnext/storage/delivery-store.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-07-16T17:14:02.910Z"}],"context":{"fact":"SETTLES the last open live question from the OAuth proxy saga: ASSIST mode — where the proxy MUTATES the request body by appending verified repo memory to the last user turn — is accepted by Anthropic on a Claude Code SUBSCRIPTION (OAuth, not API key). Live-verified 2026-07-16 with a real headless Claude Code session (claude -p) through kage proxy --mode assist: 4 memories injected per eligible request, requests succeeded (no 401/429 from the mutation), the session answered normally, and the store recorded the first real \"delivered\" context deliveries (attachment_by_provider.anthropic delivered:2).","verification":"Proxy stats: 2 req, 8 memories injected, 2 receipts, 54,078 in / 218 out tokens; the session completed normally (answered the prompt) with no 4xx from the mutated requests. kage status afterwards: attachment delivered:2 / skipped:6 / failed_open:0, attachment_by_provider.anthropic delivered:2 — the first real DELIVERED context deliveries. Same day, audit mode was validated the same way (receipts 3→6, all skipped) through a separate live session."},"freshness":{"ttl_days":365,"last_verified_at":"2026-07-16T17:14:02.910Z","path_fingerprints":[{"path":"mcp/proxy.ts","sha256":"78f6932948bb8159e5039ecfc0e448b1355dec0638555958d38866626787d585","size":26330},{"path":"mcp/vnext/adapters/anthropic-proxy.ts","sha256":"e4e4784806a1573dfb025f36e1bf79f3c236eb7354f5ef5990e776f14cb39686","size":18336},{"path":"mcp/vnext/storage/delivery-store.ts","sha256":"e2f1315776de2730eb5d950ef86c33f875bf687c003f660b135307bd7007af18","size":9689}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"stale_reasons":[],"estimated_tokens_saved":566},"created_at":"2026-07-16T17:14:02.910Z","updated_at":"2026-07-16T17:14:02.910Z","author_branch":"codex/kage-vnext-implementation"}
```

