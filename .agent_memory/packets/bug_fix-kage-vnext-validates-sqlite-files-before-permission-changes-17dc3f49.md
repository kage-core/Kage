---
type: "Bug Fix"
title: "Kage vNext validates SQLite files before permission changes"
description: "Kage vNext treats a file backed SQLite database and its wal/ shm sidecars as one filesystem trust set. It atomically precreates an absent database as 0600, then lstat classifies every existing DB/WAL/SHM path before chan"
resource: "mcp/vnext/storage/database.ts"
tags: ["session-learning", "vnext", "sqlite", "permissions", "wal", "shm", "symlink", "filesystem", "security"]
timestamp: "2026-07-14T22:00:30.012Z"
x-kage-id: "repo:kage-vnext-implementation:bug_fix:kage-vnext-validates-sqlite-files-before-permission-changes-1783956861094"
x-kage-type: "bug_fix"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-verified: "verified"
x-kage-paths: ["mcp/vnext/storage/database.ts", "mcp/vnext/storage/storage.test.ts"]
x-kage-stack: ["TypeScript", "Node.js", "SQLite"]
---

# Kage vNext validates SQLite files before permission changes

> Kage vNext treats a file backed SQLite database and its wal/ shm sidecars as one filesystem trust set. It atomically …

Kage vNext treats a file-backed SQLite database and its -wal/-shm sidecars as one filesystem trust set. It atomically precreates an absent database as 0600, then lstat-classifies every existing DB/WAL/SHM path before changing any member's mode or constructing SQLite. Only regular non-symlink files are accepted; directories, symlinks, FIFOs, devices, sockets, and other special objects receive contextual Kage vNext path errors without following or mutating their targets. Valid legacy DB/WAL/SHM files are tightened to 0600 before open, and sidecars are classified and tightened again after WAL activation inside the close-on-initialization-failure boundary. Existing parent directory modes remain unchanged.
Evidence: The initial required regressions failed 4 of 36 tests: legacy WAL remained 0644, a directory was chmodded, a symlink target was chmodded, and SQLite opened despite a sidecar symlink. A two-phase ordering regression then failed 1 of 36 until all paths were classified before any chmod. Final focused storage tests passed 36/36; npm test --prefix mcp passed 470/470 plus 12/12 dogfood tests.
Verified by: npm run build --prefix mcp; node --test mcp/dist/vnext/storage/storage.test.js; npm test --prefix mcp

## Verification

The initial required regressions failed 4 of 36 tests: legacy WAL remained 0644, a directory was chmodded, a symlink target was chmodded, and SQLite opened despite a sidecar symlink. A two-phase ordering regression then failed 1 of 36 until all paths were classified before any chmod. Final focused storage tests passed 36/36; npm test --prefix mcp passed 470/470 plus 12/12 dogfood tests.

# Citations

[1] explicit_capture (2026-07-13T15:34:21.094Z)
[2] reverification

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:kage-vnext-implementation:bug_fix:kage-vnext-validates-sqlite-files-before-permission-changes-1783956861094","title":"Kage vNext validates SQLite files before permission changes","summary":"Kage vNext treats a file backed SQLite database and its wal/ shm sidecars as one filesystem trust set. It atomically precreates an absent database as 0600, then lstat classifies every existing DB/WAL/SHM path before chan","body":"Kage vNext treats a file-backed SQLite database and its -wal/-shm sidecars as one filesystem trust set. It atomically precreates an absent database as 0600, then lstat-classifies every existing DB/WAL/SHM path before changing any member's mode or constructing SQLite. Only regular non-symlink files are accepted; directories, symlinks, FIFOs, devices, sockets, and other special objects receive contextual Kage vNext path errors without following or mutating their targets. Valid legacy DB/WAL/SHM files are tightened to 0600 before open, and sidecars are classified and tightened again after WAL activation inside the close-on-initialization-failure boundary. Existing parent directory modes remain unchanged.\nEvidence: The initial required regressions failed 4 of 36 tests: legacy WAL remained 0644, a directory was chmodded, a symlink target was chmodded, and SQLite opened despite a sidecar symlink. A two-phase ordering regression then failed 1 of 36 until all paths were classified before any chmod. Final focused storage tests passed 36/36; npm test --prefix mcp passed 470/470 plus 12/12 dogfood tests.\nVerified by: npm run build --prefix mcp; node --test mcp/dist/vnext/storage/storage.test.js; npm test --prefix mcp","type":"bug_fix","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["session-learning","vnext","sqlite","permissions","wal","shm","symlink","filesystem","security"],"paths":["mcp/vnext/storage/database.ts","mcp/vnext/storage/storage.test.ts"],"stack":["TypeScript","Node.js","SQLite"],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-07-13T15:34:21.094Z"},{"kind":"reverification","at":"2026-07-14T22:00:30.012Z","verified_by":"npm test --prefix mcp (665/665); npm run test:vnext --prefix mcp (224/224); dogfood 12/12","evidence":"Kage vNext Phase A tasks 5-8 (7f5ca5c, 860a272, 73c7a97, 4fb4390, c17682e, ba3475f, 850e7be) added the fail-open Claude hook adapter, proxy transformation receipts and measurement, the connect/status/open/receipts CLI, the phase gate, and context-delivery recording. Re-read each packet's claim against the new code: all still hold. The vNext work is ADDITIVE — setupAgent remains the single source of truth for hooks, legacy hooks still run for every event the adapter does not handle, the proxy's existing invariants (byte-identical count_tokens passthrough, untouched system prompt, last-user-turn-only injection, streaming, workspace routing, real per-request session id) are preserved and asserted by tests, no MCP tool was removed, and audit mode forwards byte-identical request bytes.","changed_paths":[{"path":"mcp/vnext/storage/storage.test.ts","prior_sha256":"a39ddad65792c8ddde6686e8d0991c73d8823520040be0d059b879226b761bb0","sha256":"126cb0b274db508b34e13c7076adbf41854234c4e199ff857c69fdb55c756c17"}]}],"context":{"fact":"Kage vNext treats a file-backed SQLite database and its -wal/-shm sidecars as one filesystem trust set. It atomically precreates an absent database as 0600, then lstat-classifies every existing DB/WAL/SHM path before changing any member's mode or constructing SQLite. Only regular non-symlink files are accepted; directories, symlinks, FIFOs, devices, sockets, and other special objects receive contextual Kage vNext path errors without following or mutating their targets. Valid legacy DB/WAL/SHM files are tightened to 0600 before open, and sidecars are classified and tightened again after WAL activation inside the close-on-initialization-failure boundary. Existing parent directory modes remain unchanged.\nEvidence: The initial required regressions failed 4 of 36 tests: legacy WAL remained 0644, a directory was chmodded, a symlink target was chmodded, and SQLite opened despite a sidecar symlink. A two-phase ordering regression then failed 1 of 36 until all paths were classified before any chmod. Final focused storage tests passed 36/36; npm test --prefix mcp passed 470/470 plus 12/12 dogfood tests.\nVerified by: npm run build --prefix mcp; node --test mcp/dist/vnext/storage/storage.test.js; npm test --prefix mcp","verification":"The initial required regressions failed 4 of 36 tests: legacy WAL remained 0644, a directory was chmodded, a symlink target was chmodded, and SQLite opened despite a sidecar symlink. A two-phase ordering regression then failed 1 of 36 until all paths were classified before any chmod. Final focused storage tests passed 36/36; npm test --prefix mcp passed 470/470 plus 12/12 dogfood tests."},"freshness":{"ttl_days":365,"last_verified_at":"2026-07-14T22:00:30.012Z","path_fingerprints":[{"path":"mcp/vnext/storage/database.ts","sha256":"5c3ccf1c72a9426ae8629ba0e8113544118af8b86d0466a699cafced1096cb8b","size":3288},{"path":"mcp/vnext/storage/storage.test.ts","sha256":"126cb0b274db508b34e13c7076adbf41854234c4e199ff857c69fdb55c756c17","size":47427}],"path_fingerprint_policy":"source_hash_staleness","verification":"evidence_reverification"},"edges":[{"relation":"references_code","to":"mcp/vnext/storage/database.ts","evidence":"agent_capture","created_at":"2026-07-13T15:34:21.094Z"},{"relation":"references_code","to":"mcp/vnext/storage/storage.test.ts","evidence":"agent_capture","created_at":"2026-07-13T15:34:21.094Z"},{"relation":"supersedes","to":"repo:kage-vnext-implementation:bug_fix:kage-vnext-storage-preserves-host-ownership-and-verifies-lossless-schema-claims-","evidence":"The replacement preserves the prior ownership, lossless JSON, and schema-validation rules while completing the DB/WAL/SHM object-classification and legacy-sidecar permission boundary.","created_at":"2026-07-13T15:34:28.319Z"}],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":100,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","concise but substantive","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":307,"reverified_at":"2026-07-14T22:00:30.012Z"},"created_at":"2026-07-13T15:34:21.094Z","updated_at":"2026-07-14T22:00:30.012Z","author_branch":"codex/kage-vnext-implementation"}
```

