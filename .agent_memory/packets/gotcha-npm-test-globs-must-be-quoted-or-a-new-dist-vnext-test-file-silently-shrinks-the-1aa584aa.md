---
type: "Gotcha"
title: "npm test globs must be quoted or a new dist/vnext test file silently shrinks the suite"
description: "mcp/package.json's test script used an UNQUOTED glob: node test dist/ / .test.js . sh has no globstar, so it expanded as a single i.e. dist/ / .test.js . Until Phase A there was no test file exactly one directory below d"
resource: "mcp/package.json"
tags: ["session-learning", "testing", "npm", "glob", "vnext", "ci"]
timestamp: "2026-07-21T14:21:44.234Z"
x-kage-id: "repo:kage-vnext-implementation:gotcha:npm-test-globs-must-be-quoted-or-a-new-dist-vnext-test-file-silently-shrinks-the"
x-kage-type: "gotcha"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-verified: "verified"
x-kage-paths: ["mcp/package.json", "mcp/vnext/phase-a-gate.test.ts"]
---

# npm test globs must be quoted or a new dist/vnext test file silently shrinks the suite

> mcp/package.json's test script used an UNQUOTED glob: node test dist/ / .test.js . sh has no globstar, so it expanded…

mcp/package.json's test script used an UNQUOTED glob: `node --test dist/**/*.test.js`. sh has no globstar, so it expanded `**` as a single `*` (i.e. `dist/*/*.test.js`). Until Phase A there was no test file exactly one directory below dist/, so the pattern matched nothing, sh passed it through literally, and node did its own (correct, recursive) globbing — 632 tests. Adding mcp/vnext/phase-a-gate.test.ts created dist/vnext/phase-a-gate.test.js, which the sh pattern DOES match, so sh expanded it to that single file and `npm test --prefix mcp` silently ran 5 tests instead of 632 while still reporting success. Fix: quote the pattern in both scripts (`node --test 'dist/**/*.test.js'`, `node --test 'dist/vnext/**/*.test.js'`) so node always does the globbing. Any future test file placed exactly one level under dist/ would have re-triggered this. Verified: quoted script runs 637 tests (632 baseline + 5 Phase A gate tests) plus 12 dogfood.
Evidence: `npm test --prefix mcp` reported 5/5 pass with the unquoted glob after adding dist/vnext/phase-a-gate.test.js, and 637/637 + 12/12 dogfood after quoting it.
Verified by: npm test --prefix mcp

## Verification

`npm test --prefix mcp` reported 5/5 pass with the unquoted glob after adding dist/vnext/phase-a-gate.test.js, and 637/637 + 12/12 dogfood after quoting it.

# Citations

[1] explicit_capture (2026-07-14T21:20:28.740Z)
[2] reverification
[3] reverification
[4] reverification
[5] reverification
[6] reverification
[7] reverification
[8] reverification
[9] reverification

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:kage-vnext-implementation:gotcha:npm-test-globs-must-be-quoted-or-a-new-dist-vnext-test-file-silently-shrinks-the","title":"npm test globs must be quoted or a new dist/vnext test file silently shrinks the suite","summary":"mcp/package.json's test script used an UNQUOTED glob: node test dist/ / .test.js . sh has no globstar, so it expanded as a single i.e. dist/ / .test.js . Until Phase A there was no test file exactly one directory below d","body":"mcp/package.json's test script used an UNQUOTED glob: `node --test dist/**/*.test.js`. sh has no globstar, so it expanded `**` as a single `*` (i.e. `dist/*/*.test.js`). Until Phase A there was no test file exactly one directory below dist/, so the pattern matched nothing, sh passed it through literally, and node did its own (correct, recursive) globbing — 632 tests. Adding mcp/vnext/phase-a-gate.test.ts created dist/vnext/phase-a-gate.test.js, which the sh pattern DOES match, so sh expanded it to that single file and `npm test --prefix mcp` silently ran 5 tests instead of 632 while still reporting success. Fix: quote the pattern in both scripts (`node --test 'dist/**/*.test.js'`, `node --test 'dist/vnext/**/*.test.js'`) so node always does the globbing. Any future test file placed exactly one level under dist/ would have re-triggered this. Verified: quoted script runs 637 tests (632 baseline + 5 Phase A gate tests) plus 12 dogfood.\nEvidence: `npm test --prefix mcp` reported 5/5 pass with the unquoted glob after adding dist/vnext/phase-a-gate.test.js, and 637/637 + 12/12 dogfood after quoting it.\nVerified by: npm test --prefix mcp","type":"gotcha","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["session-learning","testing","npm","glob","vnext","ci"],"paths":["mcp/package.json","mcp/vnext/phase-a-gate.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-07-14T21:20:28.740Z"},{"kind":"reverification","at":"2026-07-14T22:00:30.357Z","verified_by":"npm test --prefix mcp (665/665); npm run test:vnext --prefix mcp (224/224); dogfood 12/12","evidence":"Kage vNext Phase A tasks 5-8 (7f5ca5c, 860a272, 73c7a97, 4fb4390, c17682e, ba3475f, 850e7be) added the fail-open Claude hook adapter, proxy transformation receipts and measurement, the connect/status/open/receipts CLI, the phase gate, and context-delivery recording. Re-read each packet's claim against the new code: all still hold. The vNext work is ADDITIVE — setupAgent remains the single source of truth for hooks, legacy hooks still run for every event the adapter does not handle, the proxy's existing invariants (byte-identical count_tokens passthrough, untouched system prompt, last-user-turn-only injection, streaming, workspace routing, real per-request session id) are preserved and asserted by tests, no MCP tool was removed, and audit mode forwards byte-identical request bytes.","changed_paths":[{"path":"mcp/vnext/phase-a-gate.test.ts","prior_sha256":"be0a7b72709006081e24374a2bbe1248db0ce3b8385a0fea4dc41af8cac1cc59","sha256":"c776a760ef1b11e100bf3395fa750386665ea742751335291a4bf6e0eb9924a3"}]},{"kind":"reverification","at":"2026-07-15T09:58:03.011Z","verified_by":"npm test --prefix mcp (735/735); npm run test:vnext (288/288); dogfood 12/12","evidence":"Provider-neutral gateway workstream completed (through commits 11212c1 Gemini tier fix, c1b3898/7e8c69b per-provider report + test fidelity, 2292f93 checkpoint). Re-read each packet's claim against the final tree: all still hold. The per-provider report/status breaks measurement down without conflation and reports null (not zero) for zero-traffic providers; the Gemini model-in-path parseRequest(body, path?) seam is unchanged and Anthropic/OpenAI still ignore the arg; the phase gate still drives a real runtime+proxy proving byte-identical audit forward; the quoted-glob gotcha and the uncached-remainder gotcha are both still accurate (pricing.ts only gained an additive max_prompt_tokens tier ceiling). The later edits were additive hardening (tier-ceiling null pricing, realistic per-provider test fixtures) that strengthen, not contradict, these decisions.","changed_paths":[{"path":"mcp/vnext/phase-a-gate.test.ts","prior_sha256":"c776a760ef1b11e100bf3395fa750386665ea742751335291a4bf6e0eb9924a3","sha256":"9a737eda6674b216248fed6d1ca6799ec76084a47e50e77ca1d7aefa46417f72"}]},{"kind":"reverification","at":"2026-07-15T19:07:28.604Z","verified_by":"npm test --prefix mcp (746/746); npm run test:vnext; dogfood 12/12; docker node:18 legacy smoke passes","evidence":"Per-provider delivery attribution (227e3d0) + blank-guard (84f9709) + Node 18 smoke verification: migration 003 adds a nullable provider column to context_deliveries (no protocol change), proxy deliveries carry gateway.provider, hook deliveries record null (never guessed), and the report/status split attachment per provider with an unattributed bucket. Also ran the real Node 18 container smoke (passes). Re-read each packet's claim against the final tree: all still hold. These changes are ADDITIVE — the proxy's provider-neutral gateway seam, per-provider measurement reporting, the uncached-remainder token rule, the single-source-of-truth hook generation (both embedded copies kept in byte-parity), workspace routing, session-id fix, and SQLite ownership validation are all unchanged in intent; the embedded hook only gained a provider:None line.","changed_paths":[{"path":"mcp/vnext/phase-a-gate.test.ts","prior_sha256":"9a737eda6674b216248fed6d1ca6799ec76084a47e50e77ca1d7aefa46417f72","sha256":"c67f93a21998c1116409607522be87ace8be670316ded428fc1bcdbd97851029"}]},{"kind":"reverification","at":"2026-07-16T18:36:46.076Z","verified_by":"npm test --prefix mcp on 2026-07-16 collected and passed 746 tests plus 12 dogfood via the quoted glob","evidence":"mcp/package.json change in commit 2051253 only added a bench:injection script line; the test script's quoted globs ('dist/**/*.test.js') are byte-identical","changed_paths":[{"path":"mcp/package.json","prior_sha256":"d97278bccc4ab89381ff4d502603bc808198827fbbf2dabbf5f80048131b0037","sha256":"31569001db7640c4e184b05167a668437392701fa23600806c62daf80307aed1"}]},{"kind":"reverification","at":"2026-07-18T07:58:11.712Z","verified_by":"npm test --prefix mcp green during Phase B; build clean","evidence":"Phase B additive work (repo-model/compiler/okf modules, migrations 004-005, model context source) touched code near cli.ts/kernel.test.ts/package.json/measurement. Each packet's claim re-read and still holds, untouched by Phase B: the reuse-thesis-not-compression framing, merge-packet content sniffing + gc retention, the quoted-npm-test-glob gotcha, the nudge/watcher removal (hooks are the loop), and the uncached-remainder token rule are all unchanged; Phase B only added adjacent model/compiler/OKF code.","changed_paths":[{"path":"mcp/package.json","prior_sha256":"31569001db7640c4e184b05167a668437392701fa23600806c62daf80307aed1","sha256":"d0e40abeac7f0e349883843826c7364ddb93a5f7fb9ec1fd2112af3c995a6c4c"}]},{"kind":"reverification","at":"2026-07-18T19:33:17.973Z","verified_by":"npm test --prefix mcp green (1120+) during Phase C Task 1; build clean","evidence":"Phase C Task 1 (commit 92e0a22) ADDED the /v2 read-model routes (overview/features/system-map/review-items/tasks/integrations) into server.ts and a test entry to package.json; Phase D earlier extended benchmarks/README.md and the checkpoint. Each packet re-read and its claim still holds, untouched by the additive Phase C work: the injection-relevance eval is still the corpus-normalized-relevance acceptance gate; the quoted-npm-test-glob gotcha still applies (in fact the new api/*.test.js files depend on it); the repo-professionalism cleanup facts are unchanged; the resume-from-checkpoint workflow is still valid (checkpoint now also carries Phase B/D); the /v2/context endpoint's kernel-sourced-trust + byte-capped + exact-section-accounting behavior is unchanged — Task 1 only added sibling read-model routes.","changed_paths":[{"path":"mcp/package.json","prior_sha256":"d0e40abeac7f0e349883843826c7364ddb93a5f7fb9ec1fd2112af3c995a6c4c","sha256":"4232e4855f489bc6d9dfbb408d3c8a6bceb79bc75a70ba854c1711a817a0f2f9"}]},{"kind":"reverification","at":"2026-07-21T11:13:04.917Z","verified_by":"npm run build --prefix mcp clean; npm test --prefix mcp 1227/1227 + dogfood 12/12","evidence":"Phases C+D and Phase E Tasks 1-6 extended daemon.ts (serves the built portal under /app/, legacy viewer still at /viewer/), mcp/package.json (added bench:compression + test:phase-c scripts and pg/stripe/jose/embedded-postgres deps), .github/workflows/ci.yml (added the browser-journey job that SKIPS honestly without KAGE_PORTAL_URL), and the execution checkpoint (added Phase D + Phase C sections). Each packet's claim re-read and still holds: the kage cloud link + viewer Team sidebar still surface the hosted dashboard without duplicating it (the /app/ portal was added alongside, and the Phase E Task 10 default-surface cutover has NOT run yet); the npm test glob is still quoted so a new dist/vnext test file cannot silently shrink the suite (verified: test script still uses quoted 'dist/**/*.test.js', suite grew 1120 -> 1227); the Phase A gate still uses a real runtime + real proxy and its report still never fakes a zero; the repo professionalism cleanup still holds; and resuming vNext execution from the committed checkpoint is still the correct workflow (the checkpoint now records A/B/C/D complete and Phase E in progress).","changed_paths":[{"path":"mcp/package.json","prior_sha256":"4232e4855f489bc6d9dfbb408d3c8a6bceb79bc75a70ba854c1711a817a0f2f9","sha256":"19782a7ef83da74e7d173ee76762f55ebb7a0ab96f346a770dd3fcbeae9938a5"}]},{"kind":"reverification","at":"2026-07-21T14:21:44.234Z","verified_by":"npm run build --prefix mcp clean; suite green through Phase E Task 9","evidence":"Phase E Tasks 7-9 landed (billing + harden, enterprise identity/data controls + harden, deploy packaging): mcp/package.json gained test:deploy (node --test ../deploy/workspace/deploy.test.mjs — explicit single file, main glob still quoted 'dist/**/*.test.js'), ci.yml extended additively, and the Phase E plan doc's checkboxes progressed. Each packet re-read, claim unchanged: the vNext program is exactly the five-phase gated strangler migration described (A/B/C/D complete + gated, E at 9/11 with its gate pending); npm test glob quoting still protects the growing suite; the Phase A gate (real runtime + real proxy, never a faked zero) stands; the branch change-memory and professionalism-cleanup claims are unaffected by the additive workspace service.","changed_paths":[{"path":"mcp/package.json","prior_sha256":"19782a7ef83da74e7d173ee76762f55ebb7a0ab96f346a770dd3fcbeae9938a5","sha256":"0072f7aeb5a9a1624221cdd9e1ae1c9f2a28d06dea086f64759b7c569a8a5059"}]}],"context":{"fact":"mcp/package.json's test script used an UNQUOTED glob: `node --test dist/**/*.test.js`. sh has no globstar, so it expanded `**` as a single `*` (i.e. `dist/*/*.test.js`). Until Phase A there was no test file exactly one directory below dist/, so the pattern matched nothing, sh passed it through literally, and node did its own (correct, recursive) globbing — 632 tests. Adding mcp/vnext/phase-a-gate.test.ts created dist/vnext/phase-a-gate.test.js, which the sh pattern DOES match, so sh expanded it to that single file and `npm test --prefix mcp` silently ran 5 tests instead of 632 while still reporting success. Fix: quote the pattern in both scripts (`node --test 'dist/**/*.test.js'`, `node --test 'dist/vnext/**/*.test.js'`) so node always does the globbing. Any future test file placed exactly one level under dist/ would have re-triggered this. Verified: quoted script runs 637 tests (632 baseline + 5 Phase A gate tests) plus 12 dogfood.\nEvidence: `npm test --prefix mcp` reported 5/5 pass with the unquoted glob after adding dist/vnext/phase-a-gate.test.js, and 637/637 + 12/12 dogfood after quoting it.\nVerified by: npm test --prefix mcp","verification":"`npm test --prefix mcp` reported 5/5 pass with the unquoted glob after adding dist/vnext/phase-a-gate.test.js, and 637/637 + 12/12 dogfood after quoting it."},"freshness":{"ttl_days":365,"last_verified_at":"2026-07-21T14:21:44.234Z","path_fingerprints":[{"path":"mcp/package.json","sha256":"0072f7aeb5a9a1624221cdd9e1ae1c9f2a28d06dea086f64759b7c569a8a5059","size":2415},{"path":"mcp/vnext/phase-a-gate.test.ts","sha256":"c67f93a21998c1116409607522be87ace8be670316ded428fc1bcdbd97851029","size":34931}],"path_fingerprint_policy":"source_hash_staleness","verification":"evidence_reverification"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":100,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","concise but substantive","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":287,"reverified_at":"2026-07-21T14:21:44.234Z"},"created_at":"2026-07-14T21:20:28.740Z","updated_at":"2026-07-21T14:21:44.234Z","author_branch":"codex/kage-vnext-implementation"}
```

