---
type: "Decision"
title: "Kage Cloud dashboard shows the FULL packet — body, cited paths, tags — not a truncated summary, so approve/reject is an informed trust decision"
description: "Fixed a real usability gap in GET /dashboard mcp/cloud server.ts : packet cards originally showed only title + a 160 char truncated summary/body slice, which is not enough information to responsibly approve or reject a m"
resource: "mcp/cloud-server.ts"
tags: ["session-learning", "cloud", "dashboard", "ui", "review-gate", "usability", "design"]
timestamp: "2026-07-09T06:39:32.729Z"
x-kage-id: "repo:https-github-com-kage-core-kage:decision:kage-cloud-dashboard-shows-the-full-packet-body-cited-paths-tags-not-a-truncated"
x-kage-type: "decision"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-verified: "unverified"
x-kage-paths: ["mcp/cloud-server.ts"]
---

# Kage Cloud dashboard shows the FULL packet — body, cited paths, tags — not a truncated summary, so approve/reject is an informed trust decision

> Fixed a real usability gap in GET /dashboard mcp/cloud server.ts : packet cards originally showed only title + a 160 …

Fixed a real usability gap in GET /dashboard (mcp/cloud-server.ts): packet cards originally showed only title + a 160-char truncated summary/body slice, which is not enough information to responsibly approve or reject a memory claim into team-shared trust. Cards now render: the full packet body (escaped, pre-wrapped, unclamped), the packet's cited paths as inline code chips (or an explicit orange 'No cited paths — cannot be re-verified against a checkout's code' warning when there are none, since an ungrounded claim is a materially different trust decision than a grounded one), tags, and the capture date. The review-gate mechanics (self-approval blocked, a different teammate's token succeeds) are unchanged — this is purely about giving the reviewer enough information to use that gate meaningfully rather than rubber-stamping a title.
Evidence: 2 tests in mcp/cloud-server.test.ts assert the full body text and the 'No cited paths' warning render for an ungrounded packet, and that a grounded packet's cited path renders under 'Cites:' with no false warning. Live-verified in the running dev server: a real repo packet's full multi-sentence body and its cited mcp/kernel.ts path both rendered correctly, screenshotted.
Verified by: npm test: 401/401 pass; live preview_screenshot against the running kage-cloud dev server

## Verification

2 tests in mcp/cloud-server.test.ts assert the full body text and the 'No cited paths' warning render for an ungrounded packet, and that a grounded packet's cited path renders under 'Cites:' with no false warning. Live-verified in the running dev server: a real repo packet's full multi-sentence body and its cited mcp/kernel.ts path both rendered correctly, screenshotted.

# Citations

[1] explicit_capture (2026-07-09T06:39:32.729Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:https-github-com-kage-core-kage:decision:kage-cloud-dashboard-shows-the-full-packet-body-cited-paths-tags-not-a-truncated","title":"Kage Cloud dashboard shows the FULL packet — body, cited paths, tags — not a truncated summary, so approve/reject is an informed trust decision","summary":"Fixed a real usability gap in GET /dashboard mcp/cloud server.ts : packet cards originally showed only title + a 160 char truncated summary/body slice, which is not enough information to responsibly approve or reject a m","body":"Fixed a real usability gap in GET /dashboard (mcp/cloud-server.ts): packet cards originally showed only title + a 160-char truncated summary/body slice, which is not enough information to responsibly approve or reject a memory claim into team-shared trust. Cards now render: the full packet body (escaped, pre-wrapped, unclamped), the packet's cited paths as inline code chips (or an explicit orange 'No cited paths — cannot be re-verified against a checkout's code' warning when there are none, since an ungrounded claim is a materially different trust decision than a grounded one), tags, and the capture date. The review-gate mechanics (self-approval blocked, a different teammate's token succeeds) are unchanged — this is purely about giving the reviewer enough information to use that gate meaningfully rather than rubber-stamping a title.\nEvidence: 2 tests in mcp/cloud-server.test.ts assert the full body text and the 'No cited paths' warning render for an ungrounded packet, and that a grounded packet's cited path renders under 'Cites:' with no false warning. Live-verified in the running dev server: a real repo packet's full multi-sentence body and its cited mcp/kernel.ts path both rendered correctly, screenshotted.\nVerified by: npm test: 401/401 pass; live preview_screenshot against the running kage-cloud dev server","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["session-learning","cloud","dashboard","ui","review-gate","usability","design"],"paths":["mcp/cloud-server.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-07-09T06:39:32.729Z"}],"context":{"fact":"Fixed a real usability gap in GET /dashboard (mcp/cloud-server.ts): packet cards originally showed only title + a 160-char truncated summary/body slice, which is not enough information to responsibly approve or reject a memory claim into team-shared trust. Cards now render: the full packet body (escaped, pre-wrapped, unclamped), the packet's cited paths as inline code chips (or an explicit orange 'No cited paths — cannot be re-verified against a checkout's code' warning when there are none, since an ungrounded claim is a materially different trust decision than a grounded one), tags, and the capture date. The review-gate mechanics (self-approval blocked, a different teammate's token succeeds) are unchanged — this is purely about giving the reviewer enough information to use that gate meaningfully rather than rubber-stamping a title.\nEvidence: 2 tests in mcp/cloud-server.test.ts assert the full body text and the 'No cited paths' warning render for an ungrounded packet, and that a grounded packet's cited path renders under 'Cites:' with no false warning. Live-verified in the running dev server: a real repo packet's full multi-sentence body and its cited mcp/kernel.ts path both rendered correctly, screenshotted.\nVerified by: npm test: 401/401 pass; live preview_screenshot against the running kage-cloud dev server","verification":"2 tests in mcp/cloud-server.test.ts assert the full body text and the 'No cited paths' warning render for an ungrounded packet, and that a grounded packet's cited path renders under 'Cites:' with no false warning. Live-verified in the running dev server: a real repo packet's full multi-sentence body and its cited mcp/kernel.ts path both rendered correctly, screenshotted."},"freshness":{"ttl_days":365,"last_verified_at":"2026-07-09T06:39:32.729Z","path_fingerprints":[{"path":"mcp/cloud-server.ts","sha256":"907a2d50c6ef5a29a44616f4c746b283ccfc69532f1e2c39d5ba7a65b127b85d","size":20791}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[{"relation":"supersedes","to":"repo:https-github-com-kage-core-kage:decision:kage-cloud-dashboard-real-server-rendered-review-gate-ui-at-get-dashboard-with-w","evidence":"The original packet described the dashboard's card content as it existed at first ship. That content was found to be insufficient (title + truncated summary only) for a reviewer to make a real trust decision, and was fixed to show full body + cited paths + tags. The replacement packet documents the corrected, current behavior.","created_at":"2026-07-09T06:39:53.164Z"}],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":4,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":4000,"discovery_tokens_estimated":true,"score":100,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","concise but substantive","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":333,"total_uses":4,"last_accessed_at":"2026-07-10T07:27:06.285Z"},"created_at":"2026-07-09T06:39:32.729Z","updated_at":"2026-07-09T06:39:53.164Z","author_branch":"master","author_name":"Kushal Jain"}
```

