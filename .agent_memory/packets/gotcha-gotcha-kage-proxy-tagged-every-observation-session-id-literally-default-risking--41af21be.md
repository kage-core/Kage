---
type: "Gotcha"
title: "GOTCHA: kage proxy tagged every observation session_id literally 'default', risking collision with real hook sessions"
description: "proxy.ts's observe call never set session id, so kernel.ts's observationHash defaulted every proxy captured observation to the literal string session:'default'. Since observationHash is scoped by repo+type+session+...+mi"
resource: "mcp/proxy.ts"
tags: ["session-learning", "gotcha", "proxy", "session", "observation", "dedup", "fix"]
timestamp: "2026-07-08T20:41:29.665Z"
x-kage-id: "repo:https-github-com-kage-core-kage:gotcha:gotcha-kage-proxy-tagged-every-observation-session-id-literally-default-risking-"
x-kage-type: "gotcha"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-verified: "unverified"
x-kage-paths: ["mcp/proxy.ts"]
---

# GOTCHA: kage proxy tagged every observation session_id literally 'default', risking collision with real hook sessions

> proxy.ts's observe call never set session id, so kernel.ts's observationHash defaulted every proxy captured observati…

proxy.ts's observe() call never set session_id, so kernel.ts's observationHash defaulted every proxy-captured observation to the literal string session:'default'. Since observationHash is scoped by repo+type+session+...+minute-bucket, every kage proxy run ever, across restarts, on the same repo, shared ONE dedup bucket, and diverged from whatever real Claude Code session id a UserPromptSubmit hook records the SAME conversation under (a real UUID) meaning the identical prompt text could be captured twice (once via proxy tap, once via hook) with no way for hash-based dedup to catch it, since the session component differs. FIX: startProxy() now generates one stable per-process session id (proxy-<uuid> via node:crypto randomUUID) at server start and passes it on every observe() call. This does not eliminate the hook/proxy dual-capture risk when both are active for one session (the proxy has no visibility into Claude Code's real session id at the HTTP layer, and that would need client-side cooperation) but it does fix within-run dedup and makes proxy-captured observations distinguishable in .agent_memory/observations instead of colliding under a shared 'default' bucket across unrelated runs.
Evidence: New e2e test spins up a real startProxy() server against a fake upstream over an actual HTTP round-trip, then reads back the written observation via the newly-exported loadObservations() and asserts session_id matches /^proxy-/ and is not 'default'.
Verified by: npm test: 391/391 pass (proxy.test.js: 6/6)

## Verification

New e2e test spins up a real startProxy() server against a fake upstream over an actual HTTP round-trip, then reads back the written observation via the newly-exported loadObservations() and asserts session_id matches /^proxy-/ and is not 'default'.

# Citations

[1] explicit_capture (2026-07-08T20:41:29.665Z)

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:https-github-com-kage-core-kage:gotcha:gotcha-kage-proxy-tagged-every-observation-session-id-literally-default-risking-","title":"GOTCHA: kage proxy tagged every observation session_id literally 'default', risking collision with real hook sessions","summary":"proxy.ts's observe call never set session id, so kernel.ts's observationHash defaulted every proxy captured observation to the literal string session:'default'. Since observationHash is scoped by repo+type+session+...+mi","body":"proxy.ts's observe() call never set session_id, so kernel.ts's observationHash defaulted every proxy-captured observation to the literal string session:'default'. Since observationHash is scoped by repo+type+session+...+minute-bucket, every kage proxy run ever, across restarts, on the same repo, shared ONE dedup bucket, and diverged from whatever real Claude Code session id a UserPromptSubmit hook records the SAME conversation under (a real UUID) meaning the identical prompt text could be captured twice (once via proxy tap, once via hook) with no way for hash-based dedup to catch it, since the session component differs. FIX: startProxy() now generates one stable per-process session id (proxy-<uuid> via node:crypto randomUUID) at server start and passes it on every observe() call. This does not eliminate the hook/proxy dual-capture risk when both are active for one session (the proxy has no visibility into Claude Code's real session id at the HTTP layer, and that would need client-side cooperation) but it does fix within-run dedup and makes proxy-captured observations distinguishable in .agent_memory/observations instead of colliding under a shared 'default' bucket across unrelated runs.\nEvidence: New e2e test spins up a real startProxy() server against a fake upstream over an actual HTTP round-trip, then reads back the written observation via the newly-exported loadObservations() and asserts session_id matches /^proxy-/ and is not 'default'.\nVerified by: npm test: 391/391 pass (proxy.test.js: 6/6)","type":"gotcha","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["session-learning","gotcha","proxy","session","observation","dedup","fix"],"paths":["mcp/proxy.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-07-08T20:41:29.665Z"}],"context":{"fact":"proxy.ts's observe() call never set session_id, so kernel.ts's observationHash defaulted every proxy-captured observation to the literal string session:'default'. Since observationHash is scoped by repo+type+session+...+minute-bucket, every kage proxy run ever, across restarts, on the same repo, shared ONE dedup bucket, and diverged from whatever real Claude Code session id a UserPromptSubmit hook records the SAME conversation under (a real UUID) meaning the identical prompt text could be captured twice (once via proxy tap, once via hook) with no way for hash-based dedup to catch it, since the session component differs. FIX: startProxy() now generates one stable per-process session id (proxy-<uuid> via node:crypto randomUUID) at server start and passes it on every observe() call. This does not eliminate the hook/proxy dual-capture risk when both are active for one session (the proxy has no visibility into Claude Code's real session id at the HTTP layer, and that would need client-side cooperation) but it does fix within-run dedup and makes proxy-captured observations distinguishable in .agent_memory/observations instead of colliding under a shared 'default' bucket across unrelated runs.\nEvidence: New e2e test spins up a real startProxy() server against a fake upstream over an actual HTTP round-trip, then reads back the written observation via the newly-exported loadObservations() and asserts session_id matches /^proxy-/ and is not 'default'.\nVerified by: npm test: 391/391 pass (proxy.test.js: 6/6)","verification":"New e2e test spins up a real startProxy() server against a fake upstream over an actual HTTP round-trip, then reads back the written observation via the newly-exported loadObservations() and asserts session_id matches /^proxy-/ and is not 'default'."},"freshness":{"ttl_days":365,"last_verified_at":"2026-07-08T20:41:29.665Z","path_fingerprints":[{"path":"mcp/proxy.ts","sha256":"6dedc2fe25b3a7a93a47b4f41f12d26d1c15eeb090d2e45211a04fe4166217c9","size":14814,"symbols":[{"name":"startproxy","kind":"function","sha256":"d15f09bc4694ce7918c2d4f14f0f5556bd72dfcf324fa6a502fc10e1fde87d38"}]}],"path_fingerprint_policy":"source_hash_staleness","verification":"repo_local_agent_capture"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":8000,"discovery_tokens_estimated":true,"score":100,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","concise but substantive","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":381},"created_at":"2026-07-08T20:41:29.665Z","updated_at":"2026-07-08T20:41:29.665Z","author_branch":"master","author_name":"Kushal Jain"}
```

