---
type: "Decision"
title: "Background proxy lifecycle: parent-writes-after-confirm state, verified on every read"
description: "kage up starts the proxy DETACHED by default mcp/vnext/runtime/proxy daemon.ts ; kage down stops it; kage run/status discover it. Design rules future agents must preserve: 1 STATE OWNERSHIP — the parent kage up writes .a"
resource: "mcp/vnext/runtime/proxy-daemon.ts"
tags: ["session-learning", "proxy", "daemon", "kage-up", "kage-down", "lifecycle", "liveness", "stale-state"]
timestamp: "2026-07-18T10:06:48.427Z"
x-kage-id: "repo:kage-vnext-implementation:decision:background-proxy-lifecycle-parent-writes-after-confirm-state-verified-on-every-r"
x-kage-type: "decision"
x-kage-status: "approved"
x-kage-scope: "repo"
x-kage-visibility: "team"
x-kage-verified: "verified"
x-kage-paths: ["mcp/vnext/runtime/proxy-daemon.ts", "mcp/vnext/runtime/commands.ts", "mcp/cli.ts", "mcp/vnext/runtime/proxy-daemon.test.ts"]
---

# Background proxy lifecycle: parent-writes-after-confirm state, verified on every read

> kage up starts the proxy DETACHED by default mcp/vnext/runtime/proxy daemon.ts ; kage down stops it; kage run/status …

kage up starts the proxy DETACHED by default (mcp/vnext/runtime/proxy-daemon.ts); kage down stops it; kage run/status discover it. Design rules future agents must preserve: (1) STATE OWNERSHIP — the parent (kage up) writes .agent_memory/daemon/proxy.json only AFTER polling probeLocalPort confirms the detached child accepts connections; the child stays the untouched kage proxy command, so a failed start leaves no state, only the proxy.log to read. (2) VERIFIED READS — proxyDaemonState never trusts the file alone (the 860a272 lesson: SIGKILL leaves status files behind): it checks pid alive+ours+node (kill(pid,0) where ESRCH/EPERM mean not-ours, plus ps comm= includes node) AND the port accepts TCP; a record failing either check is removed and reported as stale_state_removed, and stopProxyDaemon never signals a pid from a stale record. (3) LOCATION — state lives in .agent_memory/daemon/ (plain legacy-daemon scratch dir), deliberately NOT in the 0700-leased vnext/ runtime dir, because the proxy runs on Node 18 and must not depend on a directory another component owns and secures. (4) A 0600/ownership check rejects checked-in or tampered state files (git delivers 0644, so a cloned hostile proxy.json is auto-distrusted) — untrusted files are neither believed nor deleted. (5) kage up --foreground writes no daemon state, so kage down does not manage it; a machine reboot needs one new kage up (no launchd/system service, stated in help/README instead of hidden).
Evidence: 29 new tests: proxy-daemon.test.ts covers dead-pid/live-pid-dead-port/malformed/untrusted state, start-confirm-then-write, never-listens reaped with no state, SIGKILL escalation; commands.test.ts adds an end-to-end run driving the real kage proxy child (up background -> verified state -> run auto-discovery -> down kills pid + cleans). Full suite 784/784 + dogfood 12/12; kage check 0 confirmed drift. Commit 8983d2b.
Verified by: npm test --prefix mcp (784/784 + 12/12); manual smoke: fresh up / reuse / SIGKILL-stale cleanup / down / down-again no-op on a scratch project

## Verification

29 new tests: proxy-daemon.test.ts covers dead-pid/live-pid-dead-port/malformed/untrusted state, start-confirm-then-write, never-listens reaped with no state, SIGKILL escalation; commands.test.ts adds an end-to-end run driving the real kage proxy child (up background -> verified state -> run auto-discovery -> down kills pid + cleans). Full suite 784/784 + dogfood 12/12; kage check 0 confirmed drift. Commit 8983d2b.

# Citations

[1] explicit_capture (2026-07-16T20:08:17.803Z)
[2] reverification
[3] reverification

## Kage state

Machine state for lossless round-trip; OKF consumers can ignore it.

```json kage-state
{"schema_version":2,"id":"repo:kage-vnext-implementation:decision:background-proxy-lifecycle-parent-writes-after-confirm-state-verified-on-every-r","title":"Background proxy lifecycle: parent-writes-after-confirm state, verified on every read","summary":"kage up starts the proxy DETACHED by default mcp/vnext/runtime/proxy daemon.ts ; kage down stops it; kage run/status discover it. Design rules future agents must preserve: 1 STATE OWNERSHIP — the parent kage up writes .a","body":"kage up starts the proxy DETACHED by default (mcp/vnext/runtime/proxy-daemon.ts); kage down stops it; kage run/status discover it. Design rules future agents must preserve: (1) STATE OWNERSHIP — the parent (kage up) writes .agent_memory/daemon/proxy.json only AFTER polling probeLocalPort confirms the detached child accepts connections; the child stays the untouched kage proxy command, so a failed start leaves no state, only the proxy.log to read. (2) VERIFIED READS — proxyDaemonState never trusts the file alone (the 860a272 lesson: SIGKILL leaves status files behind): it checks pid alive+ours+node (kill(pid,0) where ESRCH/EPERM mean not-ours, plus ps comm= includes node) AND the port accepts TCP; a record failing either check is removed and reported as stale_state_removed, and stopProxyDaemon never signals a pid from a stale record. (3) LOCATION — state lives in .agent_memory/daemon/ (plain legacy-daemon scratch dir), deliberately NOT in the 0700-leased vnext/ runtime dir, because the proxy runs on Node 18 and must not depend on a directory another component owns and secures. (4) A 0600/ownership check rejects checked-in or tampered state files (git delivers 0644, so a cloned hostile proxy.json is auto-distrusted) — untrusted files are neither believed nor deleted. (5) kage up --foreground writes no daemon state, so kage down does not manage it; a machine reboot needs one new kage up (no launchd/system service, stated in help/README instead of hidden).\nEvidence: 29 new tests: proxy-daemon.test.ts covers dead-pid/live-pid-dead-port/malformed/untrusted state, start-confirm-then-write, never-listens reaped with no state, SIGKILL escalation; commands.test.ts adds an end-to-end run driving the real kage proxy child (up background -> verified state -> run auto-discovery -> down kills pid + cleans). Full suite 784/784 + dogfood 12/12; kage check 0 confirmed drift. Commit 8983d2b.\nVerified by: npm test --prefix mcp (784/784 + 12/12); manual smoke: fresh up / reuse / SIGKILL-stale cleanup / down / down-again no-op on a scratch project","type":"decision","scope":"repo","visibility":"team","sensitivity":"internal","status":"approved","confidence":0.7,"tags":["session-learning","proxy","daemon","kage-up","kage-down","lifecycle","liveness","stale-state"],"paths":["mcp/vnext/runtime/proxy-daemon.ts","mcp/vnext/runtime/commands.ts","mcp/cli.ts","mcp/vnext/runtime/proxy-daemon.test.ts"],"stack":[],"source_refs":[{"kind":"explicit_capture","captured_at":"2026-07-16T20:08:17.803Z"},{"kind":"reverification","at":"2026-07-18T07:43:01.235Z","verified_by":"npm test --prefix mcp green at 926/926 + dogfood 12/12 during Phase B; build clean","evidence":"Phase B (repository model + knowledge compiler) is being added additively: new modules under mcp/vnext/repo-model/ and mcp/vnext/compiler/, storage migrations 004-005, and Phase-B code near existing files (cli.ts, kernel.test.ts). Re-read each packet's claim against the current tree — all still hold and are untouched by Phase B: the ambient-hook PATH->cli.js->npx fallback, hook version stamps + anchors, the ungrounded-utterance capture guard, the 7-area architecture-audit fixes, and the background-proxy parent-writes-after-confirm lifecycle are all unchanged in behavior; Phase B only added new evidence/claim/compiler code adjacent to them.","changed_paths":[{"path":"mcp/cli.ts","prior_sha256":"4ceedb22e58758dcf0c29fb995b43bb5de3008bbe36c968d0a7a9f045f5a451a","sha256":"ce51729e7f373b213fd9a12f986d3c31710c6063ca484c9e8e7d55d66052d3d6"}]},{"kind":"reverification","at":"2026-07-18T10:06:48.427Z","verified_by":"npm test --prefix mcp green during Phase D; build clean","evidence":"Phase D (context efficiency + reversible compression + Minimal Change Guard) is additive: new mcp/vnext/gateway/ modules (content-store, compressors, budget-engine, transform, live-zone) and proxy/cli wiring near cli.ts. Each packet's claim re-read and still holds, untouched by Phase D: the 7-area architecture-audit fixes, the background-proxy parent-writes-after-confirm lifecycle, the capture/distill rebuild-restart runbook, setupAgent single-source-of-truth hook generation, and the git-native collaborative-memory design are all unchanged; Phase D only added adjacent gateway/compression code.","changed_paths":[{"path":"mcp/cli.ts","prior_sha256":"ce51729e7f373b213fd9a12f986d3c31710c6063ca484c9e8e7d55d66052d3d6","sha256":"1dc568ab5f41069ef4991f75e164417317e83248a1cf406cc816734b7ae7a7e7"}]}],"context":{"fact":"kage up starts the proxy DETACHED by default (mcp/vnext/runtime/proxy-daemon.ts); kage down stops it; kage run/status discover it. Design rules future agents must preserve: (1) STATE OWNERSHIP — the parent (kage up) writes .agent_memory/daemon/proxy.json only AFTER polling probeLocalPort confirms the detached child accepts connections; the child stays the untouched kage proxy command, so a failed start leaves no state, only the proxy.log to read. (2) VERIFIED READS — proxyDaemonState never trusts the file alone (the 860a272 lesson: SIGKILL leaves status files behind): it checks pid alive+ours+node (kill(pid,0) where ESRCH/EPERM mean not-ours, plus ps comm= includes node) AND the port accepts TCP; a record failing either check is removed and reported as stale_state_removed, and stopProxyDaemon never signals a pid from a stale record. (3) LOCATION — state lives in .agent_memory/daemon/ (plain legacy-daemon scratch dir), deliberately NOT in the 0700-leased vnext/ runtime dir, because the proxy runs on Node 18 and must not depend on a directory another component owns and secures. (4) A 0600/ownership check rejects checked-in or tampered state files (git delivers 0644, so a cloned hostile proxy.json is auto-distrusted) — untrusted files are neither believed nor deleted. (5) kage up --foreground writes no daemon state, so kage down does not manage it; a machine reboot needs one new kage up (no launchd/system service, stated in help/README instead of hidden).\nEvidence: 29 new tests: proxy-daemon.test.ts covers dead-pid/live-pid-dead-port/malformed/untrusted state, start-confirm-then-write, never-listens reaped with no state, SIGKILL escalation; commands.test.ts adds an end-to-end run driving the real kage proxy child (up background -> verified state -> run auto-discovery -> down kills pid + cleans). Full suite 784/784 + dogfood 12/12; kage check 0 confirmed drift. Commit 8983d2b.\nVerified by: npm test --prefix mcp (784/784 + 12/12); manual smoke: fresh up / reuse / SIGKILL-stale cleanup / down / down-again no-op on a scratch project","verification":"29 new tests: proxy-daemon.test.ts covers dead-pid/live-pid-dead-port/malformed/untrusted state, start-confirm-then-write, never-listens reaped with no state, SIGKILL escalation; commands.test.ts adds an end-to-end run driving the real kage proxy child (up background -> verified state -> run auto-discovery -> down kills pid + cleans). Full suite 784/784 + dogfood 12/12; kage check 0 confirmed drift. Commit 8983d2b."},"freshness":{"ttl_days":365,"last_verified_at":"2026-07-18T10:06:48.427Z","path_fingerprints":[{"path":"mcp/vnext/runtime/proxy-daemon.ts","sha256":"89dec06b69e0a7d6e5155b463899d12d2a6385bebec462dd9c0f60915a07f5c6","size":15511,"symbols":[{"name":"probelocalport","kind":"function","sha256":"daa8d4adb574ab3ec5236fcba2eb7523114d4284a3cfa1cf08c46b00c27825d0"},{"name":"proxydaemonstate","kind":"function","sha256":"845ca80ed3e4cbec69725e88868ad85274c96ed684a2476adceb53f3724bc21c"},{"name":"stopproxydaemon","kind":"function","sha256":"c7830988747892f4cf666ba9c626c432fb6a395c06e18a13bce0dc42da9e61cd"}]},{"path":"mcp/vnext/runtime/commands.ts","sha256":"765e6e40dba9df3f6d8f215b1d82d1ed84c381140d429eae008e2906867d5cbc","size":57913},{"path":"mcp/cli.ts","sha256":"1dc568ab5f41069ef4991f75e164417317e83248a1cf406cc816734b7ae7a7e7","size":166046},{"path":"mcp/vnext/runtime/proxy-daemon.test.ts","sha256":"383971a5a8dc9d7e4b84d7982820fc4860580d63467737a022d3c38cba3890dd","size":13769}],"path_fingerprint_policy":"source_hash_staleness","verification":"evidence_reverification"},"edges":[],"quality":{"reviewer":"repo-local-agent","votes_up":0,"votes_down":0,"uses_30d":0,"reports_stale":0,"review_boundary":"git_or_pr","promotion_requires_review":true,"discovery_tokens":4000,"discovery_tokens_estimated":true,"score":94,"reasons":["high-value memory type","has source evidence","grounded to repo paths","tagged","actionable rationale or verification"],"risks":[],"duplicate_candidates":[],"estimated_tokens_saved":516,"reverified_at":"2026-07-18T10:06:48.427Z"},"created_at":"2026-07-16T20:08:17.803Z","updated_at":"2026-07-18T10:06:48.427Z","author_branch":"codex/kage-vnext-implementation"}
```

